<h1>New match</h1>
<% form_for(@match) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f} %>
  
	<div style="clear:left;" class="button-section">
    <%= f.submit "Create" %>
    <%= link_to 'Back', matches_path %> 
  </div>
<% end %>
